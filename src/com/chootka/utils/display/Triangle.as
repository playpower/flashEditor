/*** @author sgrant*/package com.chootka.utils.display {	import flash.display.Graphics;	import flash.display.Sprite;		public class Triangle extends Sprite 	{		public function Triangle(numWidth:Number, numHeight:Number, numColor:Number, strDirection:String)		{			var g:Graphics = this.graphics;						g.lineStyle(0, numColor, 0.0, true);			g.beginFill(numColor, 1.0);						switch(strDirection)			{				case "left":					g.moveTo(0, 0);					g.lineTo(0, numHeight);					g.lineTo(-numWidth, numHeight / 2);					g.lineTo(0, 0);					g.endFill();					break;									case "right":					g.moveTo(0, 0);					g.lineTo(0, numHeight);					g.lineTo(numWidth, numHeight / 2);					g.lineTo(0, 0);					g.endFill();					break;									case "up":					g.moveTo(numWidth / 2, 0);					g.lineTo(numWidth, numHeight);					g.lineTo(0, numHeight);					g.lineTo(numWidth / 2, 0);					g.endFill();					break;									case "down":					g.moveTo(0, 0);					g.lineTo(0, numWidth);					g.lineTo(numWidth / 2, numHeight);					g.lineTo(0, 0);					g.endFill();					break;			}		}	}}